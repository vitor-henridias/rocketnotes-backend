/** @type {import('jest').Config} */

const config = {
  bail: true,
  // clearMocks: true,
  coverageProvider: "v8",

  // testMatch: [
  //   "<rootDir>/src/**/*.spec.js"
  // ]
};

module.exports = config;
